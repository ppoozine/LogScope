export default function AnalyzerPage() {
  return (
    <div className="mx-auto flex max-w-md flex-col items-center justify-center gap-3 px-6 py-24 text-center">
      <h1 className="text-2xl font-semibold tracking-tight">Analyzer</h1>
      <p className="text-sm text-muted-foreground">
        VRL editor、即時 parse、Library 比對都會在這裡。即將於 spec C 開放。
      </p>
    </div>
  );
}
