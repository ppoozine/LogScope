export default function CopilotPage() {
  return (
    <div className="mx-auto flex max-w-md flex-col items-center justify-center gap-3 px-6 py-24 text-center">
      <h1 className="text-2xl font-semibold tracking-tight">Copilot</h1>
      <p className="text-sm text-muted-foreground">
        VRL 生成、Log 解釋、Library 比對三技能。即將於 spec D 開放。也可從右下角的 ✦ 按鈕直接開啟
        panel。
      </p>
    </div>
  );
}
