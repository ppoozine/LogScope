# Copilot D4 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 為 VRL compile error 加 inline 修復：lint diagnostic tooltip 內出現「✨ Fix with Copilot」按鈕，點下後 reuse D3 ghost-text infra streaming 取代出錯那一行。

**Architecture:** 沿用 D3 endpoint `/api/v1/copilot/inline/vrl`、ghost text widget、hint bar、keymap、useInlineVrl hook；加 `vrl_fix` skill + extend `InlineVrlRequest` (`skill` + `compile_error`) + 在 `vrl-lint.ts` 為 Diagnostic 加 actions。沒有新元件、沒有新 endpoint。

**Tech Stack:** FastAPI · Anthropic SDK · Pydantic · React 19 · `@codemirror/lint` · vitest · pytest-asyncio · biome.

**Spec:** `docs/superpowers/specs/2026-05-10-copilot-d4-design.md`

---

## File Structure

### Backend — modified

| Path | Change |
|---|---|
| `app/modules/copilot/constants.py` | + `SKILL_VRL_FIX = "vrl_fix"` |
| `app/modules/copilot/schemas.py` | + `InlineSkill` Literal alias；`InlineVrlRequest` 加 `skill: InlineSkill = "vrl_inline"` 與 `compile_error: str | None`；validator 擴 `vrl_fix` 規則 |
| `app/modules/copilot/services/prompt_builder.py` | + `_BLOCK1_VRL_FIX`；`build_inline_system_blocks` dispatch by `request.skill`；block 2 在 `vrl_fix` 時加 `<compile_error>` |
| `app/modules/copilot/services/chat_service.py` | `stream_inline` 改 `model=self._model_for(request.skill)`（從寫死 `"vrl_inline"` 改成依 request） |
| `app/modules/copilot/routers/chat_router.py` | DI `skill_models` 加 `vrl_fix` |

### Backend — modified tests

| Path | Change |
|---|---|
| `tests/unit/modules/copilot/test_schemas.py` | + 5 cases for `vrl_fix` validator |
| `tests/unit/modules/copilot/test_prompt_builder.py` | + 4 cases for vrl_fix block + dispatch |
| `tests/unit/modules/copilot/test_chat_service.py` | + 2 cases for vrl_fix model dispatch + system block |
| `tests/unit/modules/copilot/test_inline_router.py` | + 2 cases for vrl_fix 200 / 422 |

### Frontend — modified

| Path | Change |
|---|---|
| `web/lib/copilot/types.ts` | + `InlineSkill`；`InlineVrlRequest` 加 `skill?: InlineSkill` 與 `compile_error?: string` |
| `web/components/analyzer/vrl-lint.ts` | + `setVrlFixDispatcher`；`parseVrlDiagnostics` 為 Diagnostic 加 `actions` |
| `web/components/analyzer/analyzer-view.tsx` | + `useEffect` 註冊 fix dispatcher，把 `useInlineVrl.send` 包裝成 `(view, diag) => void` |

### Frontend — new tests

| Path | Responsibility |
|---|---|
| `web/components/analyzer/__tests__/vrl-lint.fix.test.ts` | Diagnostic.actions 結構 + `setVrlFixDispatcher` 行為 |
| `web/components/analyzer/__tests__/analyzer-view.fix.test.tsx` | mount/unmount 時 dispatcher 被注入/清除 |

---

## Task Order Map

```
T1   Backend constants — SKILL_VRL_FIX
T2   Backend schemas — extend InlineVrlRequest (skill + compile_error + validator)
T3   prompt_builder — _BLOCK1_VRL_FIX + dispatch in build_inline_system_blocks
T4   chat_service.stream_inline — model dispatch by request.skill
T5   chat_router DI — skill_models["vrl_fix"]
T6   Backend integration test for /inline/vrl with vrl_fix
T7   Frontend types — InlineSkill + skill?/compile_error?
T8   vrl-lint.ts — setVrlFixDispatcher + Diagnostic.actions + tests
T9   analyzer-view.tsx — register dispatcher useEffect + tests
T10  Acceptance smoke + regression check + PR
```

單一 PR。每 task 一次 commit。

---

## Task 1: Backend constants — `SKILL_VRL_FIX`

**Files:**
- Modify: `app/modules/copilot/constants.py`

- [ ] **Step 1.1: Append constant**

In `app/modules/copilot/constants.py`，在既有 `SKILL_VRL_INLINE = "vrl_inline"` 下方加：

```python
SKILL_VRL_FIX = "vrl_fix"
```

- [ ] **Step 1.2: Commit**

```bash
git add app/modules/copilot/constants.py
git commit -m "feat(copilot): add SKILL_VRL_FIX constant for D4"
```

---

## Task 2: Backend schemas — extend `InlineVrlRequest`

**Files:**
- Modify: `app/modules/copilot/schemas.py`
- Test: `tests/unit/modules/copilot/test_schemas.py` (extend)

- [ ] **Step 2.1: Add failing tests**

In `tests/unit/modules/copilot/test_schemas.py`，在既有 `class TestInlineVrlRequest:` 內加（避免重複 import；既有 import 已含 `pytest`、`ValidationError`、`InlineVrlRequest`）：

```python
    def test_default_skill_is_vrl_inline(self):
        r = InlineVrlRequest(
            instruction="x",
            mode="insert",
            current_vrl="",
            cursor_offset=0,
        )
        assert r.skill == "vrl_inline"

    def test_vrl_fix_valid_request(self):
        r = InlineVrlRequest(
            instruction="Fix this",
            skill="vrl_fix",
            mode="replace",
            current_vrl="abcdefghij",
            selection_start=2,
            selection_end=5,
            compile_error="error[E110]: ...",
        )
        assert r.skill == "vrl_fix"
        assert r.compile_error == "error[E110]: ..."

    def test_vrl_fix_missing_compile_error(self):
        with pytest.raises(ValidationError):
            InlineVrlRequest(
                instruction="x",
                skill="vrl_fix",
                mode="replace",
                current_vrl="abcdefghij",
                selection_start=2,
                selection_end=5,
                # compile_error omitted
            )

    def test_vrl_fix_blank_compile_error(self):
        with pytest.raises(ValidationError):
            InlineVrlRequest(
                instruction="x",
                skill="vrl_fix",
                mode="replace",
                current_vrl="abcdefghij",
                selection_start=2,
                selection_end=5,
                compile_error="   ",
            )

    def test_vrl_fix_requires_replace_mode(self):
        with pytest.raises(ValidationError):
            InlineVrlRequest(
                instruction="x",
                skill="vrl_fix",
                mode="insert",
                current_vrl="abcdefghij",
                cursor_offset=0,
                compile_error="error[E110]: ...",
            )

    def test_vrl_fix_compile_error_too_long(self):
        with pytest.raises(ValidationError):
            InlineVrlRequest(
                instruction="x",
                skill="vrl_fix",
                mode="replace",
                current_vrl="abcdefghij",
                selection_start=2,
                selection_end=5,
                compile_error="x" * 20_001,
            )
```

- [ ] **Step 2.2: Run, expect FAIL**

Run: `uv run pytest tests/unit/modules/copilot/test_schemas.py::TestInlineVrlRequest -v -k "vrl_fix or default_skill"`
Expected: FAIL — `skill` field unknown / no validator for vrl_fix

- [ ] **Step 2.3: Extend schema**

In `app/modules/copilot/schemas.py`，找到既有 `InlineMode = Literal["insert", "replace"]` 那行，在它下方加：

```python
InlineSkill = Literal["vrl_inline", "vrl_fix"]
```

在 `InlineVrlRequest` 的欄位定義中（在 `mode: InlineMode` 那行下方），加：

```python
    skill: InlineSkill = "vrl_inline"
```

在 `logs: list[str]...` 那行下方加：

```python
    compile_error: str | None = Field(default=None, max_length=20_000)
```

擴 `_check_offsets` validator（更名為 `_check` 較貼切，但為了不動 D3 既有的 method 名稱，新增獨立 validator）：

在 `_check_offsets` 方法之後（仍在 class 內）加：

```python
    @model_validator(mode="after")
    def _check_skill(self) -> "InlineVrlRequest":
        if self.skill == "vrl_fix":
            if self.compile_error is None or not self.compile_error.strip():
                raise ValueError("vrl_fix skill requires non-empty compile_error")
            if self.mode != "replace":
                raise ValueError("vrl_fix skill requires mode=replace")
        return self
```

- [ ] **Step 2.4: Run, expect PASS**

Run: `uv run pytest tests/unit/modules/copilot/test_schemas.py -v`
Expected: 全綠（既有 + 6 新測試）

- [ ] **Step 2.5: Commit**

```bash
git add app/modules/copilot/schemas.py tests/unit/modules/copilot/test_schemas.py
git commit -m "feat(copilot): extend InlineVrlRequest with vrl_fix skill + compile_error"
```

---

## Task 3: prompt_builder — `_BLOCK1_VRL_FIX` + dispatch

**Files:**
- Modify: `app/modules/copilot/services/prompt_builder.py`
- Test: `tests/unit/modules/copilot/test_prompt_builder.py` (extend)

- [ ] **Step 3.1: Add failing tests**

In `tests/unit/modules/copilot/test_prompt_builder.py`，在 `class TestBuildInlineSystemBlocks:` 內加：

```python
    def _req_fix(self, **kw):
        defaults = dict(
            instruction="Fix this",
            skill="vrl_fix",
            mode="replace",
            current_vrl="abcdefghij",
            selection_start=2,
            selection_end=5,
            compile_error="error[E110]: function `split` expected `string`, got `bytes`",
            vrl_engine="0.32",
        )
        defaults.update(kw)
        return InlineVrlRequest(**defaults)

    def test_vrl_fix_uses_dedicated_block1(self):
        blocks = build_inline_system_blocks(
            self._req_fix(), max_log_lines=20, max_vrl_chars=4000,
        )
        b1 = blocks[0]["text"]
        assert "VRL compile-error fixer" in b1
        assert "<|sel_start|>" in b1
        assert "Output ONLY raw VRL" in b1
        assert "無法修復" in b1

    def test_vrl_fix_block2_includes_compile_error(self):
        blocks = build_inline_system_blocks(
            self._req_fix(compile_error="error[E110]: WIRE_THIS_THROUGH"),
            max_log_lines=20, max_vrl_chars=4000,
        )
        b2 = blocks[1]["text"]
        assert "<compile_error>" in b2
        assert "WIRE_THIS_THROUGH" in b2
        assert "<|sel_start|>" in b2
        assert "<|sel_end|>" in b2

    def test_vrl_inline_block2_excludes_compile_error(self):
        # default skill=vrl_inline (existing behavior) must NOT include compile_error block
        blocks = build_inline_system_blocks(
            self._req_insert(),  # existing helper (skill defaults to vrl_inline)
            max_log_lines=20, max_vrl_chars=4000,
        )
        b2 = blocks[1]["text"]
        assert "<compile_error>" not in b2

    def test_vrl_fix_block1_differs_from_vrl_inline(self):
        b1_fix = build_inline_system_blocks(
            self._req_fix(), max_log_lines=20, max_vrl_chars=4000,
        )[0]["text"]
        b1_inline = build_inline_system_blocks(
            self._req_insert(), max_log_lines=20, max_vrl_chars=4000,
        )[0]["text"]
        assert b1_fix != b1_inline
```

- [ ] **Step 3.2: Run, expect FAIL**

Run: `uv run pytest tests/unit/modules/copilot/test_prompt_builder.py::TestBuildInlineSystemBlocks -v -k "vrl_fix"`
Expected: FAIL — block 1 doesn't differ / `<compile_error>` not present

- [ ] **Step 3.3: Add `_BLOCK1_VRL_FIX` and dispatch**

In `app/modules/copilot/services/prompt_builder.py`，在既有 `_BLOCK1_VRL_INLINE = """..."""` 之後加：

```python
_BLOCK1_VRL_FIX = """You are LogScope's VRL compile-error fixer. The user has VRL that
fails to compile. You output ONLY the fixed VRL that replaces the
marked region — no prose, no fence, no explanation.

# Process
1. Read <current_vrl> with the broken region wrapped in
   <|sel_start|>...<|sel_end|>.
2. Read <compile_error> — the exact diagnostic from the VRL compiler
   (typically `error[Exxx]:` block with `:line:col` location).
3. Output the minimal fix that resolves the cited error, preserving
   the original intent of the marked region.

# Output rules (strict)
- Output ONLY raw VRL that REPLACES the region between markers.
- No markdown fences, no leading/trailing prose, no comments.
- No trailing newline.
- The output should be syntactically valid VRL of the engine
  version specified in <facts><vrl_engine>.
- If you cannot determine a safe fix from the data, output exactly:
  `// 無法修復：<原因>`

# Don't
- Don't change semantics outside the marked region.
- Don't invent fields, functions, or types not in <current_vrl>.
- Don't use VRL functions outside the standard set (parse_syslog,
  parse_json, parse_key_value/parse_kv, parse_regex, parse_csv,
  split, to_int/to_float/to_bool/to_string/to_timestamp, del,
  exists, string).

# Example
<facts><vrl_engine>0.32</vrl_engine></facts>
<current_vrl><![CDATA[
. = parse_syslog!(.message)
<|sel_start|>parts = split(.message, ",")<|sel_end|>
.src_ip = parts[6]
]]></current_vrl>
<compile_error><![CDATA[
error[E110]: function "split" expected `string`, got `bytes`
  ┌─ :2:18
  │
2 │ parts = split(.message, ",")
  │              ^^^^^^^^^^^^^^^
]]></compile_error>

OUTPUT:
parts = split(string!(.message), ",")
"""
```

修改 `build_inline_system_blocks`，把：

```python
    blocks: list[dict] = [
        {
            "type": "text",
            "text": _BLOCK1_VRL_INLINE,
            "cache_control": {"type": "ephemeral"},
        }
    ]
```

改為：

```python
    block1_text = _BLOCK1_VRL_FIX if request.skill == "vrl_fix" else _BLOCK1_VRL_INLINE
    blocks: list[dict] = [
        {
            "type": "text",
            "text": block1_text,
            "cache_control": {"type": "ephemeral"},
        }
    ]
```

並在 build block 2 的 `parts` list 構造中（在 `if request.logs:` 之前）加：

```python
    if request.skill == "vrl_fix" and request.compile_error:
        parts.append(
            f"<compile_error><![CDATA[{_safe_cdata(request.compile_error)}]]></compile_error>"
        )
```

- [ ] **Step 3.4: Run, expect PASS**

Run: `uv run pytest tests/unit/modules/copilot/test_prompt_builder.py -v`
Expected: 全綠（既有 + 4 新）

- [ ] **Step 3.5: Commit**

```bash
git add app/modules/copilot/services/prompt_builder.py tests/unit/modules/copilot/test_prompt_builder.py
git commit -m "feat(copilot): add vrl_fix prompt block + dispatch in inline builder"
```

---

## Task 4: chat_service.stream_inline — model dispatch by request.skill

**Files:**
- Modify: `app/modules/copilot/services/chat_service.py`
- Test: `tests/unit/modules/copilot/test_chat_service.py` (extend)

- [ ] **Step 4.1: Add failing tests**

In `tests/unit/modules/copilot/test_chat_service.py`，在 `class TestStreamInline:` 內加：

```python
    async def test_uses_vrl_fix_model_override(self):
        svc = _service(skill_models={"vrl_fix": "fix-model", "vrl_inline": "inline-model"})
        captured = {}

        def stream_fn(**kw):
            captured.update(kw)
            return _FakeStream([])

        svc._client = MagicMock()
        svc._client.messages.stream = stream_fn

        req = InlineVrlRequest(
            instruction="x",
            skill="vrl_fix",
            mode="replace",
            current_vrl="abcdefghij",
            selection_start=2,
            selection_end=5,
            compile_error="error[E110]: ...",
        )
        _events = [b async for b in svc.stream_inline(request=req)]
        assert captured["model"] == "fix-model"

    async def test_vrl_inline_default_still_uses_inline_model(self):
        svc = _service(skill_models={"vrl_inline": "inline-model", "vrl_fix": "fix-model"})
        captured = {}

        def stream_fn(**kw):
            captured.update(kw)
            return _FakeStream([])

        svc._client = MagicMock()
        svc._client.messages.stream = stream_fn

        # default skill (no skill specified -> vrl_inline)
        _events = [b async for b in svc.stream_inline(request=_req())]
        assert captured["model"] == "inline-model"
```

- [ ] **Step 4.2: Run, expect FAIL**

Run: `uv run pytest tests/unit/modules/copilot/test_chat_service.py::TestStreamInline -v -k "vrl_fix or vrl_inline_default"`
Expected: FAIL — model is hardcoded to `_model_for("vrl_inline")` so `vrl_fix` falls back to default

- [ ] **Step 4.3: Modify stream_inline**

In `app/modules/copilot/services/chat_service.py`，找到 `stream_inline` 內：

```python
            async with self._client.messages.stream(
                model=self._model_for("vrl_inline"),
                ...
            ) as stream:
```

改為：

```python
            async with self._client.messages.stream(
                model=self._model_for(request.skill),
                ...
            ) as stream:
```

- [ ] **Step 4.4: Run, expect PASS**

Run: `uv run pytest tests/unit/modules/copilot/test_chat_service.py -v`
Expected: 全綠

- [ ] **Step 4.5: Commit**

```bash
git add app/modules/copilot/services/chat_service.py tests/unit/modules/copilot/test_chat_service.py
git commit -m "feat(copilot): stream_inline dispatches model by request.skill"
```

---

## Task 5: chat_router DI — `skill_models["vrl_fix"]`

**Files:**
- Modify: `app/modules/copilot/routers/chat_router.py`

- [ ] **Step 5.1: Add to skill_models dict**

In `app/modules/copilot/routers/chat_router.py`，找到 `skill_models` 區塊：

```python
    skill_models: dict[str, str] = {}
    if settings.llm_copilot_vrl_model:
        skill_models["vrl_generate"] = settings.llm_copilot_vrl_model
        skill_models["vrl_optimize"] = settings.llm_copilot_vrl_model
        skill_models["vrl_inline"] = settings.llm_copilot_vrl_model
```

加入 `vrl_fix`：

```python
    skill_models: dict[str, str] = {}
    if settings.llm_copilot_vrl_model:
        skill_models["vrl_generate"] = settings.llm_copilot_vrl_model
        skill_models["vrl_optimize"] = settings.llm_copilot_vrl_model
        skill_models["vrl_inline"] = settings.llm_copilot_vrl_model
        skill_models["vrl_fix"] = settings.llm_copilot_vrl_model
```

- [ ] **Step 5.2: Smoke run**

Run: `uv run pytest tests/unit/modules/copilot/ -v`
Expected: 全綠（無新測試但既有測試需通過）

- [ ] **Step 5.3: Commit**

```bash
git add app/modules/copilot/routers/chat_router.py
git commit -m "feat(copilot): wire vrl_fix into LLM_COPILOT_VRL_MODEL DI"
```

---

## Task 6: integration test — `vrl_fix` route paths

**Files:**
- Modify: `tests/unit/modules/copilot/test_inline_router.py`

- [ ] **Step 6.1: Add failing tests**

In `tests/unit/modules/copilot/test_inline_router.py`，在 `class TestInlineRoute:` 內加：

```python
    async def test_vrl_fix_returns_event_stream(
        self, app: FastAPI, client: AsyncClient
    ):
        from app.modules.copilot.routers.chat_router import get_chat_service

        fake = AsyncMock()
        fake.stream_inline = lambda **kwargs: _fake_stream_gen()
        app.dependency_overrides[get_chat_service] = lambda: fake
        app.dependency_overrides[current_user] = _user

        r = await client.post(
            "/api/v1/copilot/inline/vrl",
            json=_payload(
                skill="vrl_fix",
                mode="replace",
                cursor_offset=None,
                selection_start=0,
                selection_end=27,
                compile_error="error[E110]: foo",
            ),
        )

        assert r.status_code == 200
        assert "text/event-stream" in r.headers["content-type"]

    async def test_vrl_fix_missing_compile_error(
        self, app: FastAPI, client: AsyncClient
    ):
        from app.modules.copilot.routers.chat_router import get_chat_service

        app.dependency_overrides[get_chat_service] = lambda: AsyncMock()
        app.dependency_overrides[current_user] = _user

        # vrl_fix without compile_error → 422
        bad = _payload(
            skill="vrl_fix",
            mode="replace",
            cursor_offset=None,
            selection_start=0,
            selection_end=27,
        )
        bad = {k: v for k, v in bad.items() if v is not None}
        r = await client.post("/api/v1/copilot/inline/vrl", json=bad)

        assert r.status_code == 422
```

- [ ] **Step 6.2: Run, expect PASS**

Run: `uv run pytest tests/unit/modules/copilot/test_inline_router.py -v`
Expected: 全綠（既有 5 + 2 新）

- [ ] **Step 6.3: Commit**

```bash
git add tests/unit/modules/copilot/test_inline_router.py
git commit -m "test(copilot): integration tests for vrl_fix route"
```

---

## Task 7: Frontend types — `InlineSkill` + `skill?` + `compile_error?`

**Files:**
- Modify: `web/lib/copilot/types.ts`

- [ ] **Step 7.1: Extend InlineVrlRequest**

In `web/lib/copilot/types.ts`，找到既有 D3 段落：

```typescript
export type InlineMode = "insert" | "replace";

/** Snake-case shape sent to backend POST /api/v1/copilot/inline/vrl. */
export type InlineVrlRequest = {
  instruction: string;
  mode: InlineMode;
  current_vrl: string;
  cursor_offset?: number;
  selection_start?: number;
  selection_end?: number;
  vrl_engine: "0.25" | "0.32";
  logs: string[];
};
```

改為：

```typescript
export type InlineMode = "insert" | "replace";
export type InlineSkill = "vrl_inline" | "vrl_fix";

/** Snake-case shape sent to backend POST /api/v1/copilot/inline/vrl. */
export type InlineVrlRequest = {
  instruction: string;
  skill?: InlineSkill;
  mode: InlineMode;
  current_vrl: string;
  cursor_offset?: number;
  selection_start?: number;
  selection_end?: number;
  vrl_engine: "0.25" | "0.32";
  logs: string[];
  compile_error?: string;
};
```

- [ ] **Step 7.2: Type-check**

Run: `cd web && npx tsc --noEmit`
Expected: 無錯（既有用法不破裂——`skill` 與 `compile_error` 都是 optional）

- [ ] **Step 7.3: Commit**

```bash
git add web/lib/copilot/types.ts
git commit -m "feat(copilot): extend InlineVrlRequest types with skill + compile_error"
```

---

## Task 8: vrl-lint.ts — `setVrlFixDispatcher` + `Diagnostic.actions`

**Files:**
- Modify: `web/components/analyzer/vrl-lint.ts`
- Test: `web/components/analyzer/__tests__/vrl-lint.fix.test.ts` (new)

- [ ] **Step 8.1: Add failing test**

`web/components/analyzer/__tests__/vrl-lint.fix.test.ts`：

```typescript
import { EditorState } from "@codemirror/state";
import { EditorView } from "@codemirror/view";
import { afterEach, describe, expect, it, vi } from "vitest";

import {
  parseVrlDiagnostics,
  setVrlFixDispatcher,
} from "@/components/analyzer/vrl-lint";

afterEach(() => {
  setVrlFixDispatcher(null);
  document.body.innerHTML = "";
});

const SAMPLE_ERROR = `error[E110]: function "split" expected \`string\`, got \`bytes\`
  ┌─ :2:9
  │
2 │ parts = split(.message, ",")
  │         ^^^^^^^^^^^^^^^^^^^^
`;

function makeView(doc: string) {
  const state = EditorState.create({ doc });
  return new EditorView({ state, parent: document.body });
}

describe("parseVrlDiagnostics + Diagnostic.actions", () => {
  it("each diagnostic carries a Fix-with-Copilot action", () => {
    const view = makeView(". = parse_syslog!(.message)\nparts = split(.message, \",\")");
    const diags = parseVrlDiagnostics(SAMPLE_ERROR, view);
    expect(diags.length).toBeGreaterThan(0);
    for (const d of diags) {
      expect(d.actions).toBeDefined();
      expect(d.actions?.[0].name).toBe("✨ Fix with Copilot");
    }
    view.destroy();
  });

  it("action.apply calls the registered dispatcher", () => {
    const view = makeView(". = parse_syslog!(.message)\nparts = split(.message, \",\")");
    const dispatcher = vi.fn();
    setVrlFixDispatcher(dispatcher);

    const diags = parseVrlDiagnostics(SAMPLE_ERROR, view);
    const action = diags[0].actions?.[0];
    action?.apply(view, diags[0].from, diags[0].to);

    expect(dispatcher).toHaveBeenCalledTimes(1);
    expect(dispatcher).toHaveBeenCalledWith(view, expect.objectContaining({
      message: expect.stringContaining("E110"),
    }));
    view.destroy();
  });

  it("action.apply with no dispatcher registered is a no-op", () => {
    const view = makeView(". = parse_syslog!(.message)\nparts = split(.message, \",\")");
    setVrlFixDispatcher(null);

    const diags = parseVrlDiagnostics(SAMPLE_ERROR, view);
    const action = diags[0].actions?.[0];
    expect(() => action?.apply(view, diags[0].from, diags[0].to)).not.toThrow();
    view.destroy();
  });
});
```

- [ ] **Step 8.2: Run, expect FAIL**

Run: `cd web && npm test -- vrl-lint.fix`
Expected: FAIL — `setVrlFixDispatcher` not exported / actions undefined

- [ ] **Step 8.3: Modify vrl-lint.ts**

In `web/components/analyzer/vrl-lint.ts`，於既有 `import` 區下面加：

```typescript
type FixDispatcher = (view: EditorView, diag: Diagnostic) => void;

let _fixDispatcher: FixDispatcher | null = null;

export function setVrlFixDispatcher(dispatcher: FixDispatcher | null): void {
  _fixDispatcher = dispatcher;
}
```

修改 `parseVrlDiagnostics` 函式，在 `return diagnostics` 之前加：

```typescript
  for (const d of diagnostics) {
    d.actions = [
      {
        name: "✨ Fix with Copilot",
        apply: (v, _from, _to) => {
          _fixDispatcher?.(v, d);
        },
      },
    ];
  }
```

- [ ] **Step 8.4: Run, expect PASS**

Run: `cd web && npm test -- vrl-lint.fix`
Expected: 全綠（3 testcase）

- [ ] **Step 8.5: Type-check + lint**

Run: `cd web && npx tsc --noEmit && npx biome check components/analyzer/vrl-lint.ts`
Expected: 無錯

- [ ] **Step 8.6: Commit**

```bash
git add web/components/analyzer/vrl-lint.ts web/components/analyzer/__tests__/vrl-lint.fix.test.ts
git commit -m "feat(analyzer): vrl-lint Diagnostic.actions for Fix-with-Copilot"
```

---

## Task 9: analyzer-view.tsx — register fix dispatcher

**Files:**
- Modify: `web/components/analyzer/analyzer-view.tsx`
- Test: `web/components/analyzer/__tests__/analyzer-view.fix.test.tsx` (new)

- [ ] **Step 9.1: Add failing test**

`web/components/analyzer/__tests__/analyzer-view.fix.test.tsx`：

```typescript
import { render } from "@testing-library/react";
import { afterEach, describe, expect, it, vi } from "vitest";

vi.mock("@/components/analyzer/vrl-lint", async () => {
  const actual = await vi.importActual<typeof import("@/components/analyzer/vrl-lint")>(
    "@/components/analyzer/vrl-lint",
  );
  return {
    ...actual,
    setVrlFixDispatcher: vi.fn(),
  };
});

import { AnalyzerView } from "@/components/analyzer/analyzer-view";
import { setVrlFixDispatcher } from "@/components/analyzer/vrl-lint";

afterEach(() => {
  vi.clearAllMocks();
});

describe("AnalyzerView fix dispatcher wiring", () => {
  it("registers a dispatcher on mount", () => {
    render(<AnalyzerView preload={null} noKey={false} />);
    expect(setVrlFixDispatcher).toHaveBeenCalled();
    const arg = (setVrlFixDispatcher as unknown as { mock: { calls: [unknown[]] } })
      .mock.calls.at(-1)?.[0];
    expect(typeof arg).toBe("function");
  });

  it("clears dispatcher on unmount", () => {
    const { unmount } = render(<AnalyzerView preload={null} noKey={false} />);
    (setVrlFixDispatcher as unknown as { mock: { calls: unknown[][] } }).mock.calls.length = 0;
    unmount();
    expect(setVrlFixDispatcher).toHaveBeenCalledWith(null);
  });
});
```

- [ ] **Step 9.2: Run, expect FAIL**

Run: `cd web && npm test -- analyzer-view.fix`
Expected: FAIL — dispatcher 未註冊

- [ ] **Step 9.3: Modify analyzer-view.tsx**

In `web/components/analyzer/analyzer-view.tsx`：

加 import（於既有 `vrl-lint` 相關 import 之中合併）：

```typescript
import { setVrlFixDispatcher } from "@/components/analyzer/vrl-lint";
```

加 `useEffect`（在既有 `useInlineVrl` 與 `inlineProviders` memo 之後、return 之前）：

```typescript
useEffect(() => {
  setVrlFixDispatcher((view, diag) => {
    const line = view.state.doc.lineAt(diag.from);
    void sendInlineRef.current({
      instruction: "Fix this VRL compile error",
      skill: "vrl_fix",
      mode: "replace",
      current_vrl: view.state.doc.toString(),
      selection_start: line.from,
      selection_end: line.to,
      vrl_engine: engineVersion,
      logs: [],
      compile_error: diag.message,
    });
  });
  return () => setVrlFixDispatcher(null);
}, [engineVersion]);
```

注意：`sendInlineRef` 在 D3 已宣告（Task 18 of D3）。Effect 的 deps 只放 `engineVersion`（`logs` 不放 — fix 不需要 sample logs，避免每次 logs 變動都重註冊；effect 內部讀 `engineVersion` 是當下值已 OK）。

- [ ] **Step 9.4: Run, expect PASS**

Run: `cd web && npm test -- analyzer-view.fix`
Expected: 全綠（2 testcase）

- [ ] **Step 9.5: Run all tests + type-check + lint**

Run: `cd web && npm test && npx tsc --noEmit && npx biome check components/analyzer/analyzer-view.tsx`
Expected: 無錯

- [ ] **Step 9.6: Commit**

```bash
git add web/components/analyzer/analyzer-view.tsx web/components/analyzer/__tests__/analyzer-view.fix.test.tsx
git commit -m "feat(analyzer): wire VRL compile-error fix dispatcher into AnalyzerView"
```

---

## Task 10: Acceptance smoke + regression check + PR

- [ ] **Step 10.1: Backend test suite**

Run: `uv run pytest tests/ -m "not integration"`
Expected: 全綠

- [ ] **Step 10.2: Frontend test suite**

Run: `cd web && npm test`
Expected: 全綠

- [ ] **Step 10.3: Type-check + lint**

Run: `cd web && npx tsc --noEmit && npx biome check .`
Expected: 無錯（如有，跑 `npx biome check --write .` auto-fix）

- [ ] **Step 10.4: Manual smoke**

啟動 dev：`uv run uvicorn app.main:app --reload &` 與 `cd web && npm run dev`

依序驗證：

1. /analyzer 寫故意錯誤 VRL：
   ```
   . = parse_syslog!(.message)
   parts = split(.message, ",")
   .src_ip = parts[6]
   ```
   （`split` 在 0.32 engine 需要 `string!(.message)`）
2. 等 lint 顯紅波浪 underline（~600ms）
3. Hover diagnostic → tooltip 顯示 + "✨ Fix with Copilot" 按鈕可見
4. 點按鈕 → ghost text 開始 streaming 取代該行；原 selection range 半透明 strikethrough
5. Hint bar：「⌛ 生成中… Esc 取消」 → done 後切「✓ Tab 接受 · Esc 拒絕」
6. Tab → 該行被 fix 取代；onChange 觸發；lint 重跑（~600ms 後 diagnostic 消失或剩下個 error）
7. Esc → ghost 消失、編輯器無變化

Regression：
- 8. ⌘K cursor mode + selection mode 仍正常（D3）
- 9. Panel chat (`✦ 解釋這幾筆 log` / `✦ 生成 VRL` 等 D2 quick-buttons) 仍正常
- 10. result-pane runtime parse error 的「✦ 問 Copilot」chip 仍正常（D2）

- [ ] **Step 10.5: Push branch + open PR**

```bash
git push -u origin feat/copilot-d4
gh pr create --base feat/copilot-d3 --title "feat(copilot): D4 — VRL compile-error inline fix" --body "$(cat <<'EOF'
## Summary

D4 在 VRL 編輯器內，當 lint 偵測到 compile error 時，於 diagnostic tooltip 加入「✨ Fix with Copilot」按鈕。點下後 reuse D3 的 inline ghost text 機制 streaming 取代該行。完全獨立於 panel chat、不開新 endpoint。

- Spec: docs/superpowers/specs/2026-05-10-copilot-d4-design.md
- Plan: docs/superpowers/plans/2026-05-10-copilot-d4.md

**Base：本 PR base 是 `feat/copilot-d3`（D3 PR #10）。D3 merge 後本 PR 會 rebase 到 main。**

## What changed

### Backend
- 新 SKILL_VRL_FIX constant
- 擴 InlineVrlRequest：+`skill: "vrl_inline" | "vrl_fix"`、+`compile_error`，validator 強制 `vrl_fix → mode=replace + compile_error 必填`
- 新 _BLOCK1_VRL_FIX skill prompt
- build_inline_system_blocks dispatch by request.skill；vrl_fix 時 block 2 多一個 <compile_error> element
- stream_inline 改 model dispatch by request.skill
- chat_router DI 加 vrl_fix → LLM_COPILOT_VRL_MODEL

### Frontend
- types.ts 擴 InlineSkill + InlineVrlRequest 加 skill?/compile_error?
- vrl-lint.ts 加 setVrlFixDispatcher + Diagnostic.actions[0]={name,apply}
- analyzer-view.tsx useEffect 註冊 dispatcher，把 useInlineVrl.send 包裝成 (view, diag) => void

### Tests
- Backend +13 unit/integration tests
- Frontend +5 unit tests

## Test plan
- [x] Backend: uv run pytest tests/ -m "not integration"
- [x] Frontend: cd web && npm test
- [x] tsc + biome clean
- [ ] Manual smoke spec §8 acceptance（10 項）

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 10.6: Wait for D3 merge → rebase**

D3 PR #10 merge 後：

```bash
git checkout main
git fetch origin && git reset --hard origin/main
git checkout feat/copilot-d4
git rebase main
git push -f origin feat/copilot-d4   # 唯一允許 -f 的場景：自己的 feature branch、僅一人在用
```

PR base 改為 main（gh CLI）：

```bash
gh pr edit <D4-PR-number> --base main
```

---

## Self-Review

**Spec coverage:**

| Spec section | Task |
|---|---|
| §3.1 schema extend | T2 |
| §3.2 SKILL_VRL_FIX constant | T1 |
| §3.3 _BLOCK1_VRL_FIX prompt | T3 |
| §3.4 build_inline_system_blocks dispatch | T3 |
| §3.5 stream_inline model dispatch | T4 |
| §3.6 chat_router DI | T5 |
| §4.1 frontend types | T7 |
| §4.2 vrl-lint Diagnostic.actions | T8 |
| §4.3 analyzer-view dispatcher wiring | T9 |
| §5 data flow | T10 manual |
| §6 error handling | T2/T4/T6 unit |
| §7 testing | each task TDD + T10 |
| §8 acceptance | T10 |
| §9 risks | T9 (lineAt single line; sendInlineRef stale closure handled by ref) |

**Placeholder scan:** 無 TBD / TODO / "implement later"。Code 範例完整。

**Type consistency:**
- `InlineSkill` 在 backend (str literal) 與 frontend (TS literal) 一致
- `InlineVrlRequest` shape 對齊（schema → ts type 對應）
- `setVrlFixDispatcher` 簽章在 vrl-lint.ts 與 analyzer-view.tsx test mock 對齊
- `_BLOCK1_VRL_FIX` 字串內容在 spec §3.3 與 plan T3 完全一致

無遺漏。
