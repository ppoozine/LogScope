# Copilot D5 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 result-pane 的 runtime parse-error chip 從「點 → 開 panel → 等讀完 → 點 Insert chip」升級為「點 → 背後 streaming → 直接彈 InsertVrlDialog」。

**Architecture:** 沿用 D3 endpoint `/inline/vrl`、D2 InsertVrlDialog、D2 store.requestInsert；新 `vrl_runtime_fix` skill + 擴 InlineVrlRequest（`failing_log` / `runtime_error`）+ 新 `useInlineRuntimeFix` hook（module-level singleton 一次只一個 in-flight）+ result-pane.tsx 改造 chip。

**Tech Stack:** FastAPI · Anthropic SDK · Pydantic · React 19 · vitest · pytest-asyncio · biome.

**Spec:** `docs/superpowers/specs/2026-05-10-copilot-d5-design.md`

---

## File Structure

### Backend — modified

| Path | Change |
|---|---|
| `app/modules/copilot/constants.py` | + `SKILL_VRL_RUNTIME_FIX = "vrl_runtime_fix"` |
| `app/modules/copilot/schemas.py` | `InlineSkill` 加 `"vrl_runtime_fix"`；`InlineVrlRequest` 加 `failing_log` / `runtime_error`；validator 擴 `_check_runtime_fix` |
| `app/modules/copilot/services/prompt_builder.py` | + `_BLOCK1_VRL_RUNTIME_FIX`；`build_inline_system_blocks` dispatch by `request.skill`（vrl_runtime_fix 走整段 raw、不 inject markers）；block 2 加 `<failing_log>` / `<runtime_error>` |
| `app/modules/copilot/routers/chat_router.py` | DI `skill_models` 加 `vrl_runtime_fix` |

### Backend — modified tests

| Path | Change |
|---|---|
| `tests/unit/modules/copilot/test_schemas.py` | + 7 cases for vrl_runtime_fix validator |
| `tests/unit/modules/copilot/test_prompt_builder.py` | + 4 cases for vrl_runtime_fix block + dispatch |
| `tests/unit/modules/copilot/test_inline_router.py` | + 2 cases for vrl_runtime_fix 200 / 422 |

### Frontend — new

| Path | Responsibility |
|---|---|
| `web/lib/copilot/hooks/use-inline-runtime-fix.ts` | Module-level singleton hook：state machine + start / cancel + on-done call store.requestInsert |
| `web/lib/copilot/hooks/__tests__/use-inline-runtime-fix.test.tsx` | 6 cases for hook state machine |
| `web/components/analyzer/__tests__/result-pane.runtime-fix.test.tsx` | 6 cases for RuntimeFixChip behavior |

### Frontend — modified

| Path | Change |
|---|---|
| `web/lib/copilot/types.ts` | `InlineSkill` 加 `"vrl_runtime_fix"`；`InlineVrlRequest` 加 `failing_log?` / `runtime_error?` |
| `web/components/analyzer/result-pane.tsx` | `AskCopilotChip` → `RuntimeFixChip`（用新 hook，刪 panel chat 路徑）；`ResultPane` props 加 `currentVrl` / `vrlEngine` / `logs` |
| `web/components/analyzer/analyzer-view.tsx` | 把 `vrl` / `engineVersion` / `logs` 透過 props 傳給 `<ResultPane>` |

---

## Task Order Map

```
T1   Backend constants — SKILL_VRL_RUNTIME_FIX
T2   Backend schemas — extend InlineVrlRequest (skill + failing_log/runtime_error + validator)
T3   prompt_builder — _BLOCK1_VRL_RUNTIME_FIX + dispatch
T4   chat_router DI — skill_models["vrl_runtime_fix"]
T5   Backend integration tests (vrl_runtime_fix paths)
T6   Frontend types — InlineSkill + failing_log?/runtime_error?
T7   useInlineRuntimeFix hook + tests
T8   result-pane.tsx — RuntimeFixChip + props extension + tests
T9   analyzer-view.tsx — pass D5 context to ResultPane
T10  Acceptance smoke + regression check + PR
```

單一 PR。每 task 一次 commit。

---

## Task 1: Backend constants — `SKILL_VRL_RUNTIME_FIX`

**Files:**
- Modify: `app/modules/copilot/constants.py`

- [ ] **Step 1.1: Append constant**

In `app/modules/copilot/constants.py`，在既有 `SKILL_VRL_FIX = "vrl_fix"` 下方加：

```python
SKILL_VRL_RUNTIME_FIX = "vrl_runtime_fix"
```

- [ ] **Step 1.2: Commit**

```bash
git add app/modules/copilot/constants.py
git commit -m "feat(copilot): add SKILL_VRL_RUNTIME_FIX constant for D5"
```

---

## Task 2: Backend schemas — extend `InlineVrlRequest`

**Files:**
- Modify: `app/modules/copilot/schemas.py`
- Test: `tests/unit/modules/copilot/test_schemas.py` (extend)

- [ ] **Step 2.1: Add failing tests**

In `tests/unit/modules/copilot/test_schemas.py`，在既有 `class TestInlineVrlRequest:` 內末端加（不要動既有 17 個 D3+D4 tests；既有 imports 已含 `pytest`、`ValidationError`、`InlineVrlRequest`）：

```python
    def test_vrl_runtime_fix_valid_request(self):
        vrl = ". = parse_syslog!(.message)\nparts = split(string!(.message), \",\")"
        r = InlineVrlRequest(
            instruction="Fix this",
            skill="vrl_runtime_fix",
            mode="replace",
            current_vrl=vrl,
            selection_start=0,
            selection_end=len(vrl),
            failing_log="<134>plain syslog no csv",
            runtime_error="function call error: index out of bounds",
        )
        assert r.skill == "vrl_runtime_fix"
        assert r.failing_log == "<134>plain syslog no csv"
        assert r.runtime_error == "function call error: index out of bounds"

    def test_vrl_runtime_fix_missing_failing_log(self):
        vrl = "abcdefghij"
        with pytest.raises(ValidationError):
            InlineVrlRequest(
                instruction="x",
                skill="vrl_runtime_fix",
                mode="replace",
                current_vrl=vrl,
                selection_start=0,
                selection_end=len(vrl),
                # failing_log omitted
                runtime_error="some error",
            )

    def test_vrl_runtime_fix_missing_runtime_error(self):
        vrl = "abcdefghij"
        with pytest.raises(ValidationError):
            InlineVrlRequest(
                instruction="x",
                skill="vrl_runtime_fix",
                mode="replace",
                current_vrl=vrl,
                selection_start=0,
                selection_end=len(vrl),
                failing_log="some log",
                # runtime_error omitted
            )

    def test_vrl_runtime_fix_blank_failing_log(self):
        vrl = "abcdefghij"
        with pytest.raises(ValidationError):
            InlineVrlRequest(
                instruction="x",
                skill="vrl_runtime_fix",
                mode="replace",
                current_vrl=vrl,
                selection_start=0,
                selection_end=len(vrl),
                failing_log="   ",
                runtime_error="some error",
            )

    def test_vrl_runtime_fix_requires_replace_mode(self):
        vrl = "abcdefghij"
        with pytest.raises(ValidationError):
            InlineVrlRequest(
                instruction="x",
                skill="vrl_runtime_fix",
                mode="insert",
                current_vrl=vrl,
                cursor_offset=0,
                failing_log="some log",
                runtime_error="some error",
            )

    def test_vrl_runtime_fix_requires_full_selection(self):
        vrl = "abcdefghij"
        with pytest.raises(ValidationError):
            InlineVrlRequest(
                instruction="x",
                skill="vrl_runtime_fix",
                mode="replace",
                current_vrl=vrl,
                selection_start=2,                  # not 0
                selection_end=len(vrl),
                failing_log="some log",
                runtime_error="some error",
            )

    def test_vrl_runtime_fix_requires_non_empty_current_vrl(self):
        with pytest.raises(ValidationError):
            InlineVrlRequest(
                instruction="x",
                skill="vrl_runtime_fix",
                mode="replace",
                current_vrl="",                     # empty
                selection_start=0,
                selection_end=0,
                failing_log="some log",
                runtime_error="some error",
            )
```

- [ ] **Step 2.2: Run, expect FAIL**

Run: `uv run pytest tests/unit/modules/copilot/test_schemas.py::TestInlineVrlRequest -v -k "vrl_runtime"`
Expected: FAIL — `skill` literal 不認 vrl_runtime_fix / `failing_log` / `runtime_error` 欄位不存在

- [ ] **Step 2.3: Extend schema**

In `app/modules/copilot/schemas.py`：

找到既有 `InlineSkill = Literal["vrl_inline", "vrl_fix"]` 那行，改為：

```python
InlineSkill = Literal["vrl_inline", "vrl_fix", "vrl_runtime_fix"]
```

在 `InlineVrlRequest` class 內，在既有 `compile_error: str | None = Field(default=None, max_length=20_000)` 下方加：

```python
    failing_log: str | None = Field(default=None, max_length=20_000)
    runtime_error: str | None = Field(default=None, max_length=20_000)
```

在既有 `_check_skill` 方法（D4 加的 vrl_fix validator）之後加新 validator：

```python
    @model_validator(mode="after")
    def _check_runtime_fix(self) -> "InlineVrlRequest":
        if self.skill == "vrl_runtime_fix":
            if self.failing_log is None or not self.failing_log.strip():
                raise ValueError("vrl_runtime_fix skill requires non-empty failing_log")
            if self.runtime_error is None or not self.runtime_error.strip():
                raise ValueError("vrl_runtime_fix skill requires non-empty runtime_error")
            if self.mode != "replace":
                raise ValueError("vrl_runtime_fix skill requires mode=replace")
            if self.selection_start != 0 or self.selection_end != len(self.current_vrl):
                raise ValueError(
                    "vrl_runtime_fix skill requires selection to cover entire current_vrl "
                    "(selection_start=0, selection_end=len(current_vrl))"
                )
            if not self.current_vrl.strip():
                raise ValueError("vrl_runtime_fix skill requires non-empty current_vrl")
        return self
```

- [ ] **Step 2.4: Run, expect PASS**

Run: `uv run pytest tests/unit/modules/copilot/test_schemas.py -v`
Expected: 全綠（既有 + 7 新測試）

- [ ] **Step 2.5: Commit**

```bash
git add app/modules/copilot/schemas.py tests/unit/modules/copilot/test_schemas.py
git commit -m "feat(copilot): extend InlineVrlRequest with vrl_runtime_fix skill + failing_log/runtime_error"
```

---

## Task 3: prompt_builder — `_BLOCK1_VRL_RUNTIME_FIX` + dispatch

**Files:**
- Modify: `app/modules/copilot/services/prompt_builder.py`
- Test: `tests/unit/modules/copilot/test_prompt_builder.py` (extend)

- [ ] **Step 3.1: Add failing tests**

In `tests/unit/modules/copilot/test_prompt_builder.py`，在 `class TestBuildInlineSystemBlocks:` 內末端加：

```python
    def _req_runtime_fix(self, **kw):
        vrl = ". = parse_syslog!(.message)\nparts = split(string!(.message), \",\")\n.src_ip = parts[6]"
        defaults = dict(
            instruction="Fix runtime",
            skill="vrl_runtime_fix",
            mode="replace",
            current_vrl=vrl,
            selection_start=0,
            selection_end=len(vrl),
            failing_log="<134>plain-syslog-no-csv",
            runtime_error="function call error: index 6 out of bounds (length: 1)",
            vrl_engine="0.32",
        )
        defaults.update(kw)
        return InlineVrlRequest(**defaults)

    def test_vrl_runtime_fix_uses_dedicated_block1(self):
        blocks = build_inline_system_blocks(
            self._req_runtime_fix(), max_log_lines=20, max_vrl_chars=4000,
        )
        b1 = blocks[0]["text"]
        assert "runtime parse-error fixer" in b1
        assert "Output ONLY raw VRL" in b1
        assert "無法修復" in b1

    def test_vrl_runtime_fix_block2_includes_failing_log_and_runtime_error(self):
        blocks = build_inline_system_blocks(
            self._req_runtime_fix(
                failing_log="WIRE_FAILING_LOG",
                runtime_error="WIRE_RUNTIME_ERROR",
            ),
            max_log_lines=20, max_vrl_chars=4000,
        )
        b2 = blocks[1]["text"]
        assert "<failing_log>" in b2
        assert "WIRE_FAILING_LOG" in b2
        assert "<runtime_error>" in b2
        assert "WIRE_RUNTIME_ERROR" in b2

    def test_vrl_runtime_fix_block2_no_selection_markers(self):
        # vrl_runtime_fix 整段 replace，current_vrl 不該有 <|sel_*|> markers
        blocks = build_inline_system_blocks(
            self._req_runtime_fix(), max_log_lines=20, max_vrl_chars=4000,
        )
        b2 = blocks[1]["text"]
        assert "<|sel_start|>" not in b2
        assert "<|sel_end|>" not in b2
        assert "<|cursor|>" not in b2

    def test_vrl_inline_and_vrl_fix_block2_excludes_failing_log(self):
        # other skills must NOT include <failing_log> or <runtime_error>
        b2_inline = build_inline_system_blocks(
            self._req_insert(), max_log_lines=20, max_vrl_chars=4000,
        )[1]["text"]
        assert "<failing_log>" not in b2_inline
        assert "<runtime_error>" not in b2_inline
```

- [ ] **Step 3.2: Run, expect FAIL**

Run: `uv run pytest tests/unit/modules/copilot/test_prompt_builder.py::TestBuildInlineSystemBlocks -v -k "vrl_runtime"`
Expected: FAIL

- [ ] **Step 3.3: Add `_BLOCK1_VRL_RUNTIME_FIX` and dispatch**

In `app/modules/copilot/services/prompt_builder.py`，在既有 `_BLOCK1_VRL_FIX = """..."""` 之後加：

```python
_BLOCK1_VRL_RUNTIME_FIX = """You are LogScope's runtime parse-error fixer. The user's VRL compiles
but fails to parse a specific log. You output ONLY a complete rewritten
VRL that handles the failing log — no prose, no fence, no explanation.

# Context
You receive:
- <current_vrl>: the user's current VRL (no markers; you replace it whole)
- <failing_log>: the specific log line that failed to parse
- <runtime_error>: the VRL runtime error message
- <logs>: a sample of all logs (for context — confirm fix doesn't break others)

# Process
1. Read <runtime_error>. Identify root cause:
   - missing field that current_vrl assumes exists
   - wrong type (e.g., field is array not string)
   - parse_* call failed on a structurally different log subtype
2. Read <failing_log> and <logs>. Confirm the field/structure that
   trips current_vrl.
3. Rewrite <current_vrl> minimally to handle the failing case:
   - prefer `??` fallback over removing logic
   - prefer `if exists()` over rewriting whole flow
   - DO NOT remove field extractions that work for other logs
4. Output the COMPLETE rewritten VRL. The output replaces all of
   <current_vrl>.

# Output rules (strict)
- Output ONLY raw VRL. No markdown fences, no prose, no comments.
- No leading/trailing newline.
- The output must be syntactically valid VRL of the engine version
  in <facts><vrl_engine>.
- If you cannot determine a fix from the data, output exactly:
  `// 無法修復：<原因>`

# Don't
- Don't invent fields not in <logs>.
- Don't hard-code values from <failing_log> as constants.
- Don't use VRL functions outside the standard set (parse_syslog,
  parse_json, parse_key_value/parse_kv, parse_regex, parse_csv,
  split, to_int/to_float/to_bool/to_string/to_timestamp, del,
  exists, string).

# Example
<facts><vrl_engine>0.32</vrl_engine></facts>
<current_vrl><![CDATA[
. = parse_syslog!(.message)
parts = split(string!(.message), ",")
.src_ip = parts[6]
]]></current_vrl>
<failing_log><![CDATA[
<134>Jan 15 plain-syslog-no-csv-tail
]]></failing_log>
<runtime_error><![CDATA[
function call error for "split": index 6 out of bounds (length: 1)
]]></runtime_error>

OUTPUT:
. = parse_syslog!(.message)
parts = split(string!(.message), ",")
.src_ip = parts[6] ?? null
"""
```

修改 `build_inline_system_blocks`，把既有 dispatch ternary：

```python
    block1_text = _BLOCK1_VRL_FIX if request.skill == "vrl_fix" else _BLOCK1_VRL_INLINE
```

改為：

```python
    if request.skill == "vrl_fix":
        block1_text = _BLOCK1_VRL_FIX
    elif request.skill == "vrl_runtime_fix":
        block1_text = _BLOCK1_VRL_RUNTIME_FIX
    else:
        block1_text = _BLOCK1_VRL_INLINE
```

接著修改 block 2 的構造邏輯。既有大致是：

```python
    safe_vrl = _sanitize_markers(request.current_vrl)
    marked_vrl = _inject_marker(safe_vrl, request)
    kept, truncated = _truncate_keeping_marker(marked_vrl, max_vrl_chars, request)

    parts: list[str] = [f"<facts><vrl_engine>{request.vrl_engine}</vrl_engine></facts>"]
    if kept is not None:
        attr = f' truncated_to="{max_vrl_chars}"' if truncated else ""
        parts.append(
            f"<current_vrl{attr}><![CDATA[{_safe_cdata(kept)}]]></current_vrl>"
        )
    if request.skill == "vrl_fix" and request.compile_error:
        parts.append(
            f"<compile_error><![CDATA[{_safe_cdata(request.compile_error)}]]></compile_error>"
        )
    if request.logs:
        ...
```

替換為：

```python
    parts: list[str] = [f"<facts><vrl_engine>{request.vrl_engine}</vrl_engine></facts>"]

    if request.skill == "vrl_runtime_fix":
        # No marker injection — selection covers whole VRL, replaced wholesale.
        kept = request.current_vrl
        if len(kept) > max_vrl_chars:
            attr = f' truncated_to="{max_vrl_chars}"'
            kept = kept[:max_vrl_chars]
        else:
            attr = ""
        parts.append(
            f"<current_vrl{attr}><![CDATA[{_safe_cdata(kept)}]]></current_vrl>"
        )
        parts.append(
            f"<failing_log><![CDATA[{_safe_cdata(request.failing_log or '')}]]></failing_log>"
        )
        parts.append(
            f"<runtime_error><![CDATA[{_safe_cdata(request.runtime_error or '')}]]></runtime_error>"
        )
    else:
        # vrl_inline / vrl_fix — inject markers, optionally drop block if marker truncated out
        safe_vrl = _sanitize_markers(request.current_vrl)
        marked_vrl = _inject_marker(safe_vrl, request)
        kept, truncated = _truncate_keeping_marker(marked_vrl, max_vrl_chars, request)
        if kept is not None:
            attr = f' truncated_to="{max_vrl_chars}"' if truncated else ""
            parts.append(
                f"<current_vrl{attr}><![CDATA[{_safe_cdata(kept)}]]></current_vrl>"
            )
        if request.skill == "vrl_fix" and request.compile_error:
            parts.append(
                f"<compile_error><![CDATA[{_safe_cdata(request.compile_error)}]]></compile_error>"
            )

    if request.logs:
        showing = min(len(request.logs), max_log_lines)
        parts.append(f'<logs count="{len(request.logs)}" showing="{showing}">')
        for i, raw in enumerate(request.logs[:max_log_lines]):
            parts.append(
                f'  <log index="{i + 1}"><![CDATA[{_safe_cdata(raw)}]]></log>'
            )
        parts.append("</logs>")

    blocks.append({"type": "text", "text": "\n".join(parts)})
    return blocks
```

- [ ] **Step 3.4: Run, expect PASS**

Run: `uv run pytest tests/unit/modules/copilot/test_prompt_builder.py -v`
Expected: 全綠（既有 + 4 新）

- [ ] **Step 3.5: Commit**

```bash
git add app/modules/copilot/services/prompt_builder.py tests/unit/modules/copilot/test_prompt_builder.py
git commit -m "feat(copilot): add vrl_runtime_fix prompt block + dispatch"
```

---

## Task 4: chat_router DI — `skill_models["vrl_runtime_fix"]`

**Files:**
- Modify: `app/modules/copilot/routers/chat_router.py`

- [ ] **Step 4.1: Add to skill_models dict**

In `app/modules/copilot/routers/chat_router.py`，找到既有 skill_models 區塊：

```python
    skill_models: dict[str, str] = {}
    if settings.llm_copilot_vrl_model:
        skill_models["vrl_generate"] = settings.llm_copilot_vrl_model
        skill_models["vrl_optimize"] = settings.llm_copilot_vrl_model
        skill_models["vrl_inline"] = settings.llm_copilot_vrl_model
        skill_models["vrl_fix"] = settings.llm_copilot_vrl_model
```

加 `vrl_runtime_fix`：

```python
    skill_models: dict[str, str] = {}
    if settings.llm_copilot_vrl_model:
        skill_models["vrl_generate"] = settings.llm_copilot_vrl_model
        skill_models["vrl_optimize"] = settings.llm_copilot_vrl_model
        skill_models["vrl_inline"] = settings.llm_copilot_vrl_model
        skill_models["vrl_fix"] = settings.llm_copilot_vrl_model
        skill_models["vrl_runtime_fix"] = settings.llm_copilot_vrl_model
```

- [ ] **Step 4.2: Smoke run**

Run: `uv run pytest tests/unit/modules/copilot/ -v`
Expected: 全綠

- [ ] **Step 4.3: Commit**

```bash
git add app/modules/copilot/routers/chat_router.py
git commit -m "feat(copilot): wire vrl_runtime_fix into LLM_COPILOT_VRL_MODEL DI"
```

---

## Task 5: integration test — `vrl_runtime_fix` route paths

**Files:**
- Modify: `tests/unit/modules/copilot/test_inline_router.py`

- [ ] **Step 5.1: Add failing tests**

In `tests/unit/modules/copilot/test_inline_router.py`，在 `class TestInlineRoute:` 內末端加（既有 7 個 tests + helpers `_user`、`_payload`、`_fake_stream_gen` 都已存在）：

```python
    async def test_vrl_runtime_fix_returns_event_stream(
        self, app: FastAPI, client: AsyncClient
    ):
        from app.modules.copilot.routers.chat_router import get_chat_service

        fake = AsyncMock()
        fake.stream_inline = lambda **kwargs: _fake_stream_gen()
        app.dependency_overrides[get_chat_service] = lambda: fake
        app.dependency_overrides[current_user] = _user

        vrl = ". = parse_syslog!(.message)"
        r = await client.post(
            "/api/v1/copilot/inline/vrl",
            json=_payload(
                skill="vrl_runtime_fix",
                mode="replace",
                cursor_offset=None,
                current_vrl=vrl,
                selection_start=0,
                selection_end=len(vrl),
                failing_log="<134>plain syslog",
                runtime_error="function call error",
            ),
        )

        assert r.status_code == 200
        assert "text/event-stream" in r.headers["content-type"]

    async def test_vrl_runtime_fix_missing_failing_log(
        self, app: FastAPI, client: AsyncClient
    ):
        from app.modules.copilot.routers.chat_router import get_chat_service

        app.dependency_overrides[get_chat_service] = lambda: AsyncMock()
        app.dependency_overrides[current_user] = _user

        vrl = ". = parse_syslog!(.message)"
        bad = _payload(
            skill="vrl_runtime_fix",
            mode="replace",
            cursor_offset=None,
            current_vrl=vrl,
            selection_start=0,
            selection_end=len(vrl),
            runtime_error="some error",
            # failing_log omitted
        )
        bad = {k: v for k, v in bad.items() if v is not None}
        r = await client.post("/api/v1/copilot/inline/vrl", json=bad)

        assert r.status_code == 422
```

- [ ] **Step 5.2: Run, expect PASS**

Run: `uv run pytest tests/unit/modules/copilot/test_inline_router.py -v`
Expected: 全綠（既有 7 + 2 新 = 9）

- [ ] **Step 5.3: Commit**

```bash
git add tests/unit/modules/copilot/test_inline_router.py
git commit -m "test(copilot): integration tests for vrl_runtime_fix route"
```

---

## Task 6: Frontend types — `InlineSkill` + `failing_log?` + `runtime_error?`

**Files:**
- Modify: `web/lib/copilot/types.ts`

- [ ] **Step 6.1: Extend InlineVrlRequest**

In `web/lib/copilot/types.ts`，找到既有 D4 段落：

```typescript
export type InlineSkill = "vrl_inline" | "vrl_fix";
```

改為：

```typescript
export type InlineSkill = "vrl_inline" | "vrl_fix" | "vrl_runtime_fix";
```

在 `InlineVrlRequest` type 末端（在 `compile_error?: string;` 後）加：

```typescript
  failing_log?: string;
  runtime_error?: string;
```

- [ ] **Step 6.2: Type-check**

Run: `cd web && npx tsc --noEmit`
Expected: 無錯（既有 callers 不破裂）

- [ ] **Step 6.3: Commit**

```bash
git add web/lib/copilot/types.ts
git commit -m "feat(copilot): extend InlineVrlRequest types with vrl_runtime_fix + failing_log/runtime_error"
```

---

## Task 7: useInlineRuntimeFix hook

**Files:**
- Create: `web/lib/copilot/hooks/use-inline-runtime-fix.ts`
- Test: `web/lib/copilot/hooks/__tests__/use-inline-runtime-fix.test.tsx`

- [ ] **Step 7.1: Add failing test**

`web/lib/copilot/hooks/__tests__/use-inline-runtime-fix.test.tsx`：

```typescript
import { act, renderHook } from "@testing-library/react";
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";

import { useInlineRuntimeFix } from "@/lib/copilot/hooks/use-inline-runtime-fix";
import { useCopilotStore } from "@/lib/copilot/store";

const BASE_ARGS = {
  chipId: "chip-A",
  currentVrl: ". = parse_syslog!(.message)",
  failingLog: "<134>plain",
  runtimeError: "function call error",
  vrlEngine: "0.32" as const,
  logs: ["<134>plain", "<134>another"],
};

function makeFetchResponse(body: string, status = 200): Response {
  const encoder = new TextEncoder();
  return new Response(
    new ReadableStream({
      start(c) {
        c.enqueue(encoder.encode(body));
        c.close();
      },
    }),
    { status, headers: { "content-type": "text/event-stream" } },
  );
}

describe("useInlineRuntimeFix", () => {
  let fetchSpy: ReturnType<typeof vi.fn>;

  beforeEach(() => {
    fetchSpy = vi.fn();
    globalThis.fetch = fetchSpy as unknown as typeof fetch;
    useCopilotStore.setState({ pendingInsert: null });
  });

  afterEach(() => {
    vi.restoreAllMocks();
  });

  it("transitions streaming → idle and calls requestInsert on done", async () => {
    fetchSpy.mockResolvedValue(
      makeFetchResponse(
        'event: text_delta\ndata: {"text":"FIXED_VRL"}\n\nevent: done\ndata: {}\n\n',
      ),
    );
    const { result } = renderHook(() => useInlineRuntimeFix());

    await act(async () => {
      await result.current.start(BASE_ARGS);
    });

    expect(result.current.state.kind).toBe("idle");
    const pending = useCopilotStore.getState().pendingInsert;
    expect(pending?.proposedVrl).toBe("FIXED_VRL");
  });

  it("transitions to error on SSE error event", async () => {
    fetchSpy.mockResolvedValue(
      makeFetchResponse(
        'event: error\ndata: {"code":"x","message":"boom"}\n\nevent: done\ndata: {}\n\n',
      ),
    );
    const { result } = renderHook(() => useInlineRuntimeFix());

    await act(async () => {
      await result.current.start(BASE_ARGS);
    });

    expect(result.current.state.kind).toBe("error");
    if (result.current.state.kind === "error") {
      expect(result.current.state.message).toBe("boom");
      expect(result.current.state.chipId).toBe("chip-A");
    }
  });

  it("error state when current_vrl is blank — does not call fetch", async () => {
    const { result } = renderHook(() => useInlineRuntimeFix());

    await act(async () => {
      await result.current.start({ ...BASE_ARGS, currentVrl: "   " });
    });

    expect(fetchSpy).not.toHaveBeenCalled();
    expect(result.current.state.kind).toBe("error");
  });

  it("cancel aborts streaming and goes idle", async () => {
    let release: (() => void) | undefined;
    fetchSpy.mockImplementation(
      () =>
        new Promise<Response>((resolve, reject) => {
          release = () => reject(Object.assign(new Error("aborted"), { name: "AbortError" }));
        }),
    );
    const { result } = renderHook(() => useInlineRuntimeFix());

    const startPromise = act(async () => {
      await result.current.start(BASE_ARGS);
    });
    // give microtask a tick
    await Promise.resolve();
    expect(result.current.state.kind).toBe("streaming");

    act(() => {
      result.current.cancel();
    });
    release?.();
    await startPromise;

    expect(result.current.state.kind).toBe("idle");
  });

  it("starting a second chip aborts the first and switches state to second", async () => {
    let releaseA: (() => void) | undefined;
    fetchSpy.mockImplementationOnce(
      () =>
        new Promise<Response>((_resolve, reject) => {
          releaseA = () => reject(Object.assign(new Error("aborted"), { name: "AbortError" }));
        }),
    );
    fetchSpy.mockImplementationOnce(() =>
      Promise.resolve(makeFetchResponse("event: done\ndata: {}\n\n")),
    );
    const { result } = renderHook(() => useInlineRuntimeFix());

    const a = act(async () => {
      await result.current.start({ ...BASE_ARGS, chipId: "chip-A" });
    });
    await Promise.resolve();

    const b = act(async () => {
      await result.current.start({ ...BASE_ARGS, chipId: "chip-B" });
    });
    releaseA?.();
    await a;
    await b;

    expect(["idle", "error"]).toContain(result.current.state.kind);
    if (result.current.state.kind === "error") {
      // The second start either yielded done OR errored on done with empty buffer ("回應為空")
      // Both are acceptable terminal states for chip-B; not chip-A.
      expect(result.current.state.chipId).toBe("chip-B");
    }
  });

  it("empty buffer at done yields error '回應為空'", async () => {
    fetchSpy.mockResolvedValue(makeFetchResponse("event: done\ndata: {}\n\n"));
    const { result } = renderHook(() => useInlineRuntimeFix());

    await act(async () => {
      await result.current.start(BASE_ARGS);
    });

    expect(result.current.state.kind).toBe("error");
    if (result.current.state.kind === "error") {
      expect(result.current.state.message).toBe("回應為空");
    }
  });
});
```

- [ ] **Step 7.2: Run, expect FAIL**

Run: `cd web && npm test -- use-inline-runtime-fix`
Expected: FAIL（找不到模組）

- [ ] **Step 7.3: Create hook**

`web/lib/copilot/hooks/use-inline-runtime-fix.ts`：

```typescript
import { useCallback, useEffect, useState } from "react";

import { streamInlineVrl } from "@/lib/copilot/inline-vrl-client";
import { useCopilotStore } from "@/lib/copilot/store";

type RuntimeFixState =
  | { kind: "idle" }
  | { kind: "streaming"; abort: AbortController; chipId: string }
  | { kind: "error"; message: string; chipId: string };

let _state: RuntimeFixState = { kind: "idle" };
const _listeners = new Set<() => void>();

function setState(next: RuntimeFixState) {
  _state = next;
  for (const l of _listeners) l();
}

export type RuntimeFixArgs = {
  chipId: string;
  currentVrl: string;
  failingLog: string;
  runtimeError: string;
  vrlEngine: "0.25" | "0.32";
  logs: string[];
};

export function useInlineRuntimeFix() {
  const [, force] = useState(0);

  useEffect(() => {
    const l = () => force((n) => n + 1);
    _listeners.add(l);
    return () => {
      _listeners.delete(l);
      if (_listeners.size === 0 && _state.kind === "streaming") {
        _state.abort.abort();
        _state = { kind: "idle" };
      }
    };
  }, []);

  const requestInsert = useCopilotStore((s) => s.requestInsert);

  const start = useCallback(
    async (args: RuntimeFixArgs) => {
      if (_state.kind === "streaming") {
        _state.abort.abort();
      }

      if (!args.currentVrl.trim()) {
        setState({ kind: "error", message: "VRL 為空、無法修復", chipId: args.chipId });
        return;
      }

      const controller = new AbortController();
      setState({ kind: "streaming", abort: controller, chipId: args.chipId });

      let buffer = "";
      try {
        for await (const ev of streamInlineVrl(
          {
            instruction: "Fix this runtime parse error",
            skill: "vrl_runtime_fix",
            mode: "replace",
            current_vrl: args.currentVrl,
            selection_start: 0,
            selection_end: args.currentVrl.length,
            vrl_engine: args.vrlEngine,
            logs: args.logs,
            failing_log: args.failingLog,
            runtime_error: args.runtimeError,
          },
          controller.signal,
        )) {
          if (_state.kind !== "streaming" || _state.chipId !== args.chipId) {
            // Superseded by another start() — drop further events
            return;
          }
          if (ev.type === "text_delta") {
            buffer += ev.text;
          } else if (ev.type === "error") {
            setState({ kind: "error", message: ev.message, chipId: args.chipId });
            return;
          } else if (ev.type === "done") {
            if (buffer.trim()) {
              requestInsert(buffer, `runtime-fix-${args.chipId}-${Date.now()}`);
              setState({ kind: "idle" });
            } else {
              setState({ kind: "error", message: "回應為空", chipId: args.chipId });
            }
            return;
          }
        }
      } catch (err) {
        if (_state.kind === "streaming" && _state.chipId !== args.chipId) {
          return; // superseded
        }
        if ((err as Error).name === "AbortError") {
          setState({ kind: "idle" });
          return;
        }
        setState({ kind: "error", message: "連線中斷", chipId: args.chipId });
      }
    },
    [requestInsert],
  );

  const cancel = useCallback(() => {
    if (_state.kind === "streaming") {
      _state.abort.abort();
      setState({ kind: "idle" });
    }
  }, []);

  return { state: _state, start, cancel };
}
```

- [ ] **Step 7.4: Run, expect PASS**

Run: `cd web && npm test -- use-inline-runtime-fix`
Expected: 全綠（6 testcase）

- [ ] **Step 7.5: Commit**

```bash
git add web/lib/copilot/hooks/use-inline-runtime-fix.ts web/lib/copilot/hooks/__tests__/use-inline-runtime-fix.test.tsx
git commit -m "feat(copilot): D5 useInlineRuntimeFix hook (singleton state machine)"
```

---

## Task 8: result-pane.tsx — RuntimeFixChip + props extension

**Files:**
- Modify: `web/components/analyzer/result-pane.tsx`
- Test: `web/components/analyzer/__tests__/result-pane.runtime-fix.test.tsx` (new)

- [ ] **Step 8.1: Add failing test**

`web/components/analyzer/__tests__/result-pane.runtime-fix.test.tsx`：

```typescript
import { fireEvent, render, screen } from "@testing-library/react";
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";

import { ResultPane } from "@/components/analyzer/result-pane";
import { useCopilotStore } from "@/lib/copilot/store";

const startMock = vi.fn();
const cancelMock = vi.fn();
let mockState: { kind: "idle" } | { kind: "streaming"; chipId: string } | {
  kind: "error";
  message: string;
  chipId: string;
} = { kind: "idle" };

vi.mock("@/lib/copilot/hooks/use-inline-runtime-fix", () => ({
  useInlineRuntimeFix: () => ({
    state: mockState,
    start: startMock,
    cancel: cancelMock,
  }),
}));

const ERROR_RESULT = {
  index: 0,
  input: "<134>failing log line",
  status: "error" as const,
  output: null,
  error: "function call error: index out of bounds",
};

const PARSE_RESULT_ERROR = {
  kind: "ok" as const,
  engine: "0.32" as const,
  compile_error: null,
  summary: { total: 1, success: 0, error: 1 },
  results: [ERROR_RESULT],
};

const BASE_PROPS = {
  parseResult: PARSE_RESULT_ERROR,
  fields: [],
  hasLogTypeContext: false,
  currentVrl: ". = parse_syslog!(.message)",
  vrlEngine: "0.32" as const,
  logs: ["<134>failing log line", "<134>good log"],
};

beforeEach(() => {
  startMock.mockReset();
  cancelMock.mockReset();
  mockState = { kind: "idle" };
  useCopilotStore.setState({ pendingInsert: null });
});

afterEach(() => {
  // expand the error result card to surface the chip
});

describe("RuntimeFixChip in ResultPane", () => {
  it("renders 修復 chip in default state", () => {
    render(<ResultPane {...BASE_PROPS} />);
    const chip = screen.getByRole("button", { name: /修復/ });
    expect(chip.textContent).toContain("✨");
  });

  it("clicking chip calls start with full args", () => {
    render(<ResultPane {...BASE_PROPS} />);
    fireEvent.click(screen.getByRole("button", { name: /修復/ }));
    expect(startMock).toHaveBeenCalledTimes(1);
    const args = startMock.mock.calls[0][0];
    expect(args.failingLog).toBe(ERROR_RESULT.input);
    expect(args.runtimeError).toBe(ERROR_RESULT.error);
    expect(args.currentVrl).toBe(BASE_PROPS.currentVrl);
    expect(args.vrlEngine).toBe(BASE_PROPS.vrlEngine);
    expect(args.logs).toEqual(BASE_PROPS.logs);
    expect(typeof args.chipId).toBe("string");
    expect(args.chipId.length).toBeGreaterThan(0);
  });

  it("displays spinner when state is streaming for this chip", () => {
    const expectedChipId = `0-${ERROR_RESULT.input.slice(0, 16)}`;
    mockState = { kind: "streaming", chipId: expectedChipId };
    render(<ResultPane {...BASE_PROPS} />);
    const chip = screen.getByRole("button");
    expect(chip.textContent).toContain("生成中");
  });

  it("displays error label when state is error for this chip", () => {
    const expectedChipId = `0-${ERROR_RESULT.input.slice(0, 16)}`;
    mockState = { kind: "error", message: "boom", chipId: expectedChipId };
    render(<ResultPane {...BASE_PROPS} />);
    const chip = screen.getByRole("button");
    expect(chip.textContent).toContain("boom");
  });

  it("clicking chip while streaming this chip calls cancel", () => {
    const expectedChipId = `0-${ERROR_RESULT.input.slice(0, 16)}`;
    mockState = { kind: "streaming", chipId: expectedChipId };
    render(<ResultPane {...BASE_PROPS} />);
    fireEvent.click(screen.getByRole("button"));
    expect(cancelMock).toHaveBeenCalledTimes(1);
  });

  it("does NOT show streaming label for a different chipId", () => {
    mockState = { kind: "streaming", chipId: "different-chip" };
    render(<ResultPane {...BASE_PROPS} />);
    const chip = screen.getByRole("button");
    expect(chip.textContent).not.toContain("生成中");
    expect(chip.textContent).toContain("修復");
  });
});
```

- [ ] **Step 8.2: Run, expect FAIL**

Run: `cd web && npm test -- result-pane.runtime-fix`
Expected: FAIL — props missing / chip 不認

- [ ] **Step 8.3: Modify result-pane.tsx**

Read the current file first to understand the existing helper structure.

In `web/components/analyzer/result-pane.tsx`：

**Imports** — replace these existing imports：

```typescript
import { useCopilot } from "@/components/providers/copilot-context";
import { useStreamingChat } from "@/lib/copilot/hooks/use-streaming-chat";
```

with：

```typescript
import { useInlineRuntimeFix } from "@/lib/copilot/hooks/use-inline-runtime-fix";
```

**ResultPane Props** — add three new fields:

```typescript
type Props = {
  parseResult: ParseResponse | null;
  fields: FieldSchemaRead[];
  onSaveBackToLibrary?: () => void;
  onSaveAsSample?: () => void;
  hasLogTypeContext: boolean;
  currentVrl: string;
  vrlEngine: "0.25" | "0.32";
  logs: string[];
};
```

Update the destructuring of the function signature to pull out the new props.

**Pass through ResultBody / ResultCard chain** — update internal helper signatures:

```typescript
function ResultBody({
  parseResult,
  filter,
  fields,
  currentVrl,
  vrlEngine,
  logs,
}: {
  parseResult: ParseResponse | null;
  filter: string;
  fields: FieldSchemaRead[];
  currentVrl: string;
  vrlEngine: "0.25" | "0.32";
  logs: string[];
}) {
  // ... existing body ...
  // In the .map callback:
  // <ResultCard key={r.index} result={r} filter={filter} fields={fields}
  //             currentVrl={currentVrl} vrlEngine={vrlEngine} logs={logs} />
}

function ResultCard({
  result,
  filter,
  fields,
  currentVrl,
  vrlEngine,
  logs,
}: {
  result: ParseResultItem;
  filter: string;
  fields: FieldSchemaRead[];
  currentVrl: string;
  vrlEngine: "0.25" | "0.32";
  logs: string[];
}) {
  // ... existing body ...
  // Replace <AskCopilotChip ... /> with:
  //   <RuntimeFixChip
  //     index={result.index}
  //     input={result.input}
  //     error={result.error ?? ""}
  //     currentVrl={currentVrl}
  //     vrlEngine={vrlEngine}
  //     logs={logs}
  //   />
}
```

**Replace AskCopilotChip with RuntimeFixChip** — delete the existing `AskCopilotChip` function entirely and replace with:

```typescript
function RuntimeFixChip({
  index,
  input,
  error,
  currentVrl,
  vrlEngine,
  logs,
}: {
  index: number;
  input: string;
  error: string;
  currentVrl: string;
  vrlEngine: "0.25" | "0.32";
  logs: string[];
}) {
  const chipId = `${index}-${input.slice(0, 16)}`;
  const { state, start, cancel } = useInlineRuntimeFix();
  const isThis =
    (state.kind === "streaming" || state.kind === "error") &&
    state.chipId === chipId;
  const isStreaming = isThis && state.kind === "streaming";
  const isError = isThis && state.kind === "error";

  const handle = () => {
    if (isStreaming) {
      cancel();
    } else {
      start({
        chipId,
        currentVrl,
        failingLog: input,
        runtimeError: error,
        vrlEngine,
        logs,
      });
    }
  };

  let label = "✨ 修復";
  let cls = "border-purple-300 bg-purple-50 text-purple-800 hover:bg-purple-100";
  if (isStreaming) {
    label = "⌛ 生成中… (點取消)";
    cls = "border-amber-300 bg-amber-50 text-amber-800";
  } else if (isError && state.kind === "error") {
    label = `⚠ ${state.message}（點重試）`;
    cls = "border-red-300 bg-red-50 text-red-800";
  }

  return (
    <button
      type="button"
      onClick={handle}
      className={cn(
        "inline-flex items-center gap-1 rounded border px-2 py-1 text-[11px]",
        cls,
      )}
    >
      {label}
    </button>
  );
}
```

**Update ResultPane body** — pass the new props to ResultBody:

```tsx
<ResultBody
  parseResult={parseResult}
  filter={filter}
  fields={fields}
  currentVrl={currentVrl}
  vrlEngine={vrlEngine}
  logs={logs}
/>
```

- [ ] **Step 8.4: Run, expect PASS**

Run: `cd web && npm test -- result-pane.runtime-fix`
Expected: 全綠（6 testcase）

- [ ] **Step 8.5: Type-check + lint**

Run: `cd web && npx tsc --noEmit && npx biome check components/analyzer/result-pane.tsx`
Expected: 無錯（如有 unused import 從 panel chat 移除路徑遺留，刪掉）

注意：`useCopilot` 和 `useStreamingChat` 已不再使用，可從 import 移除。

- [ ] **Step 8.6: Commit**

```bash
git add web/components/analyzer/result-pane.tsx web/components/analyzer/__tests__/result-pane.runtime-fix.test.tsx
git commit -m "feat(analyzer): replace AskCopilotChip with RuntimeFixChip (D5)"
```

---

## Task 9: analyzer-view.tsx — pass D5 context to ResultPane

**Files:**
- Modify: `web/components/analyzer/analyzer-view.tsx`

- [ ] **Step 9.1: Update ResultPane invocation**

In `web/components/analyzer/analyzer-view.tsx`，找到既有的 `<ResultPane ... />` 處（在 `diffMode ? ... : <ResultPane ... />` 三元運算內）：

```tsx
<ResultPane
  parseResult={parseData ?? null}
  fields={fields}
  hasLogTypeContext={!!logTypeId}
  onSaveBackToLibrary={logTypeId ? handleSaveBackToLibrary : undefined}
  onSaveAsSample={logTypeId ? () => setSampleDialogOpen(true) : undefined}
/>
```

加新 props：

```tsx
<ResultPane
  parseResult={parseData ?? null}
  fields={fields}
  hasLogTypeContext={!!logTypeId}
  onSaveBackToLibrary={logTypeId ? handleSaveBackToLibrary : undefined}
  onSaveAsSample={logTypeId ? () => setSampleDialogOpen(true) : undefined}
  currentVrl={vrl}
  vrlEngine={engineVersion}
  logs={logs ? logs.split("\n").filter((l) => l.length > 0) : []}
/>
```

注意：`vrl`、`engineVersion`、`logs` state 都已存在於 analyzer-view 內（D1 既有）。

- [ ] **Step 9.2: Run all tests + type-check + lint**

Run: `cd web && npm test && npx tsc --noEmit && npx biome check components/analyzer/analyzer-view.tsx`
Expected: 全綠（無新測試但既有測試需通過）

- [ ] **Step 9.3: Commit**

```bash
git add web/components/analyzer/analyzer-view.tsx
git commit -m "feat(analyzer): pass D5 context (vrl/engine/logs) to ResultPane"
```

---

## Task 10: Acceptance smoke + regression check + PR

- [ ] **Step 10.1: Backend test suite**

Run: `uv run pytest tests/ -m "not integration"`
Expected: 全綠

- [ ] **Step 10.2: Frontend test suite**

Run: `cd web && npm test`
Expected: 全綠

- [ ] **Step 10.3: Type-check + lint**

Run: `cd web && npx tsc --noEmit && npx biome check .`
Expected: 無錯（如有 formatting issue 跑 `npx biome check --write .` auto-fix 並 commit）

- [ ] **Step 10.4: Manual smoke**

啟動 dev：`uv run uvicorn app.main:app --reload &` 與 `cd web && npm run dev`

依序驗證：

1. /analyzer 寫 VRL：
   ```
   . = parse_syslog!(.message)
   parts = split(string!(.message), ",")
   .src_ip = parts[6]
   ```
2. 餵 logs（多行）：
   ```
   <134>Jan 15 10:23:45 fw01 1,2024/01/15,007901,TRAFFIC,end,2049,10.0.1.5,8.8.8.8
   <134>Jan 15 10:23:46 fw01 plain syslog no csv tail
   ```
3. 等 ~400ms parse → result-pane 顯示 1 success + 1 error
4. 第二筆 error card 應自動展開、顯示「✨ 修復」chip（替代既有的「✦ 問 Copilot 怎麼修」）
5. 點 chip → chip 變「⌛ 生成中… (點取消)」、**panel 不開**
6. 等 ~3-10 秒 → InsertVrlDialog 自動彈出
7. 確認 dialog diff：current vs proposed（proposed 應含類似 `?? null` 的 fallback）
8. Confirm → editor 換新 VRL → parse 重跑 → 第二筆變 success
9. 驗 cancel：再 trigger，streaming 中點 chip → chip 變回原狀、無 dialog
10. 驗 Esc cancel via dialog：trigger → done → dialog 開 → Cancel → editor 不變

Regression：
- 11. D1 panel chat（⌘\\ 打開）+ 解釋 log + log_explain 仍正常
- 12. D2 quick-buttons（解釋這幾筆 log / 生成 VRL / 比對 Library / 最佳化 VRL / 找異常值）仍正常
- 13. D3 ⌘K cursor + selection 仍正常
- 14. D4 lint diagnostic「✨ Fix with Copilot」仍正常
- 15. D2 Insert dialog 從 panel 路徑（生成 VRL → Insert chip → dialog）仍正常

- [ ] **Step 10.5: Push branch + open PR**

```bash
git push -u origin feat/copilot-d5
gh pr create --base main --title "feat(copilot): D5 — runtime parse-error inline fix" --body "$(cat <<'EOF'
## Summary

D5 把 result-pane 的 runtime parse-error chip（既有 D2 AskCopilotChip：點 → 開 panel → 等讀完 → 點 Insert chip）升級為 inline UX：點 → 背後 streaming → InsertVrlDialog 自動彈出。完全跳過 panel chat 步驟。

新 vrl_runtime_fix skill；沿用 /api/v1/copilot/inline/vrl endpoint（透過 skill dispatch）；reuse D2 InsertVrlDialog + store.requestInsert；新 useInlineRuntimeFix hook (module-level singleton)。

- Spec: docs/superpowers/specs/2026-05-10-copilot-d5-design.md
- Plan: docs/superpowers/plans/2026-05-10-copilot-d5.md (10 task TDD)

## What changed

### Backend
- 新 SKILL_VRL_RUNTIME_FIX constant
- 擴 InlineVrlRequest：+`vrl_runtime_fix` skill literal、+`failing_log`/`runtime_error` 欄位、validator 強制 selection 涵蓋整段 + non-empty current_vrl + 兩個 error 欄位都要
- 新 _BLOCK1_VRL_RUNTIME_FIX skill prompt（rewrite whole VRL）
- build_inline_system_blocks dispatch by request.skill；vrl_runtime_fix 走 raw current_vrl（不 inject markers）+ block 2 加 \`<failing_log>\` / \`<runtime_error>\`
- chat_router DI 加 vrl_runtime_fix → LLM_COPILOT_VRL_MODEL

### Frontend
- types.ts 擴 InlineSkill + failing_log? + runtime_error?
- 新 useInlineRuntimeFix hook（module-level singleton；start / cancel；done → store.requestInsert；同時只一個 in-flight；abort race 有 chipId 保護）
- result-pane.tsx 把 AskCopilotChip 替換為 RuntimeFixChip；ResultPane props 加 currentVrl / vrlEngine / logs
- analyzer-view.tsx 把 vrl / engineVersion / logs 傳給 ResultPane

## Test plan
- [x] Backend: uv run pytest tests/ -m "not integration"
- [x] Frontend: cd web && npm test
- [x] tsc + biome clean
- [ ] Manual smoke per spec §8 acceptance（15 項，含 D1-D4 regression）

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

---

## Self-Review

**Spec coverage:**

| Spec section | Task |
|---|---|
| §3.1 schema extend | T2 |
| §3.2 SKILL_VRL_RUNTIME_FIX constant | T1 |
| §3.3 _BLOCK1_VRL_RUNTIME_FIX prompt | T3 |
| §3.4 prompt_builder dispatch | T3 |
| §3.5 chat_service (no change) | (D4 already covers) |
| §3.6 chat_router DI | T4 |
| §4.1 frontend types | T6 |
| §4.2 useInlineRuntimeFix hook | T7 |
| §4.3 result-pane RuntimeFixChip | T8 |
| §4.3 analyzer-view pass props | T9 |
| §5 data flow | T8 + T10 manual |
| §6 error handling | T2 / T3 / T5 / T7 (hook tests) |
| §7 testing | each task TDD + T10 |
| §8 acceptance criteria | T10 |
| §9 module structure | covered task-by-task |
| §10 risks | T7 hook handles superseded race; T8 handles chipId compare |

**Placeholder scan:** 無 TBD / TODO / "implement later"。Code 範例完整。

**Type consistency:**
- `InlineSkill` 在 backend (Python Literal) 與 frontend (TS union) 一致
- `RuntimeFixState` shape 在 hook 內 / 測試內對齊
- `RuntimeFixArgs` 簽章在 hook 與 RuntimeFixChip 對齊（chipId, currentVrl, failingLog, runtimeError, vrlEngine, logs）
- `_BLOCK1_VRL_RUNTIME_FIX` prompt 與 spec §3.3 verbatim
- `failing_log` / `runtime_error` 欄位名稱在 schema (snake_case) 與 frontend types (snake_case) 對齊

無遺漏。
